name: Rhynode Finance iOS Build (unsigned IPA)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to build"
        required: true
        default: "feat/web-redesign-2026-07-10"

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 90
    defaults:
      run:
        working-directory: ./rhynode-finance/apps/mobile
    env:
      EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY }}
      EXPO_PUBLIC_API_URL: https://rhynode-finance.vercel.app
      EXPO_PUBLIC_POSTHOG_HOST: https://us.i.posthog.com
      SENTRY_DISABLE_AUTO_UPLOAD: true
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.2.2

      - name: Install monorepo dependencies
        working-directory: ./rhynode-finance
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Prebuild iOS project
        run: pnpm exec expo prebuild -p ios --clean

      - name: Install CocoaPods
        working-directory: ./rhynode-finance/apps/mobile/ios
        run: pod install --repo-update

      - name: Patch fmt pod for Xcode 26
        working-directory: ./rhynode-finance/apps/mobile/ios/Pods/fmt
        run: |
          # fmt 11.0.2 with Xcode 26 Apple Clang fails on consteval format-string checking.
          # Disable the consteval path and replace FMT_STRING with a runtime string so
          # format-inl.cc compiles without requiring a newer fmt release.
          sed -i '' 's/#define FMT_USE_CONSTEVAL 1/#define FMT_USE_CONSTEVAL 0/g' include/fmt/base.h || true
          sed -i '' 's/#define FMT_USE_CONSTEVAL 1/#define FMT_USE_CONSTEVAL 0/g' include/fmt/format.h || true
          sed -i '' 's/#define FMT_CONSTEVAL consteval/#define FMT_CONSTEVAL/' include/fmt/base.h || true
          sed -i '' 's/#define FMT_CONSTEXPR20 constexpr/#define FMT_CONSTEXPR20/' include/fmt/base.h || true

          awk '
            /^#define FMT_STRING\(s\)/ { print "#undef FMT_STRING"; print "#define FMT_STRING(s) (s)"; skip=1; next }
            skip && /^#define FMT_STRING/ { next }
            skip && !/^ *\\/ { skip=0 }
            { print }
          ' include/fmt/format.h > /tmp/format.h.new && mv /tmp/format.h.new include/fmt/format.h

          echo "Patched fmt headers for Xcode 26"

      - name: Build unsigned iOS archive
        working-directory: ./rhynode-finance/apps/mobile/ios
        run: |
          xcodebuild -workspace Rhynode.xcworkspace \
            -scheme Rhynode \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath ../build/Rhynode.xcarchive \
            archive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_STYLE=Manual \
            PROVISIONING_PROFILE_SPECIFIER="" \
            DEVELOPMENT_TEAM=""

      - name: Package unsigned IPA
        working-directory: ./rhynode-finance/apps/mobile
        run: |
          mkdir -p build/Payload
          cp -R build/Rhynode.xcarchive/Products/Applications/Rhynode.app build/Payload/Rhynode.app
          cd build && zip -r Rhynode-unsigned.ipa Payload

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: rhynode-ios-unsigned
          path: rhynode-finance/apps/mobile/build/Rhynode-unsigned.ipa
          retention-days: 7
